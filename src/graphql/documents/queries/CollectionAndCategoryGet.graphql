#import "./../fragments/ProductDetailsFragment.graphql"

query CollectionAndCategoryhGet($slug: String) {
	collection(slug: $slug) {
		name
		products {
			...ProductDetails
		}
	}
	category(slug: $slug) {
		name
		products {
			...ProductDetails
		}
	}
}
